export const metadata = {
    title: "Smart Society",
    description: "Community Maintenance & Complaint Management System",
  };
  
  export default function RootLayout({ children }) {
    return (
      <html lang="en">
        <body>{children}</body>
      </html>
    );
  }